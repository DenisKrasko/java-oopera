public class Choreographer extends Person{
    private final int shoulderWidth;

    public Choreographer(String name, String surname, Gender gender, int shoulderWidth) {
        super(name, surname, gender);
        this.shoulderWidth = shoulderWidth;
    }
}
